<h1>
  <%= @asked_question.title %>
</h1>

<%= form_for @user_answer, url: {action: "answer"} do |f| %>
  <% @answers_possible.each do |possible_answer| %>
    <%= f.radio_button(:possible_answer_id, possible_answer.id) %> <%= possible_answer.content %><br/>
    <%= f.radio_button(:possible_answer_id, possible_answer.id) %> <%= possible_answer.content %><br/>
    <!-- #comment afficher la prochaine possible_answer? -->
  <% end %>
  <%= f.submit "> Following question" %>
<% end %>

<!-- possible_answer_id: integer -->
